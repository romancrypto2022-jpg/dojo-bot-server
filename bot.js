// ═══════════════════════════════════════════════
// DOJO Leadership OS — Bot Server
// Хостинг: Railway.app
// Функции:
//   1. Отвечает на /start REF_CODE
//   2. Генерирует токен для авто-входа на сайт
//   3. Отправляет ежедневные уведомления (cron)
//   4. /notify — принимает запросы от браузера (без токена в клиенте)
// ═══════════════════════════════════════════════

const TelegramBot = require('node-telegram-bot-api');
const cron        = require('node-cron');
const express     = require('express');
const crypto      = require('crypto');

// ── НАСТРОЙКИ ──────────────────────────────────
const BOT_TOKEN        = process.env.BOT_TOKEN;
const NOTIFY_SECRET    = process.env.NOTIFY_SECRET; // новый секрет, задаётся в Railway
const FIREBASE_PROJECT = 'dojo-leadership';
const DOJO_URL         = 'https://romansmolkov.com/dojo/app';

// ── FIREBASE REST ───────────────────────────────
async function fsGet(path) {
  const r = await fetch(`https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT}/databases/(default)/documents/${path}`);
  return r.ok ? await r.json() : null;
}

async function fsSet(path, fields) {
  const body = { fields: {} };
  for (const [k, v] of Object.entries(fields)) {
    if (typeof v === 'number') body.fields[k] = { integerValue: v };
    else body.fields[k] = { stringValue: String(v ?? '') };
  }
  await fetch(
    `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT}/databases/(default)/documents/${path}`,
    { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }
  );
}

async function fsDelete(path) {
  await fetch(
    `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT}/databases/(default)/documents/${path}`,
    { method: 'DELETE' }
  );
}

async function getAllUsers() {
  const data = await fsGet('users');
  if (!data?.documents) return [];
  return data.documents.map(doc => {
    const f = doc.fields || {};
    return {
      uid:      f.uid?.stringValue,
      name:     f.name?.stringValue || 'Партнёр',
      chatId:   f.chatId?.stringValue,
      streak:   parseInt(f.currentStreak?.integerValue || 0),
      lastDate: f.lastActiveDate?.stringValue || '',
      invitedBy: f.invitedBy?.stringValue || null,
      goal: {
        income:   f.goal?.mapValue?.fields?.income?.stringValue   || null,
        maingoal: f.goal?.mapValue?.fields?.maingoal?.stringValue || null,
        dream:    f.goal?.mapValue?.fields?.dream?.stringValue    || null,
        forwhom:  f.goal?.mapValue?.fields?.forwhom?.stringValue  || null,
        reason:   f.goal?.mapValue?.fields?.reason?.stringValue   || null,
      }
    };
  }).filter(u => u.chatId);
}

// ── КОНТЕНТ ─────────────────────────────────────
const THOUGHTS = [
  "Каждый день без действий работает против твоей цели.",
  "Среди твоих контактов уже есть будущий лидер твоей команды.",
  "Твоя жизнь через год определяется тем, что ты делаешь сегодня.",
  "Дисциплина — это форма уважения к своим целям.",
  "Маленькое действие каждый день сильнее одного большого усилия раз в месяц.",
  "Рост — это не событие. Это ежедневная практика.",
  "Первый шаг не должен быть идеальным. Он должен быть сделан.",
  "Настоящий лидер не ждёт мотивации. Он создаёт её действием.",
  "Страх — это компас. Он показывает где находится твой рост.",
  "Лидерство — это решение которое принимается каждый день.",
  "Команда отражает лидера. Хочешь другую — стань другим лидером.",
  "Приглашение — это не продажа. Это подарок возможности.",
  "Самая дорогая цена — это сожаление о несделанном.",
  "Стабильность побеждает интенсивность.",
  "Твой следующий лидер уже в твоём телефоне. Просто напиши.",
  "Результаты — это сумма ежедневных решений.",
  "Свобода строится не за один день. Но строится если делать каждый день.",
  "Доход определяется ценностью которую ты создаёшь для других.",
  "Люди которые изменили мир тоже когда-то не знали с чего начать.",
  "Возможности не исчезают. Их забирают те кто действует.",
  "Ты не строишь бизнес. Ты строишь человека который способен его вести.",
  "Сравнивай себя только с тем кем ты был вчера.",
  "Один разговор может изменить чью-то жизнь. Проведи его сегодня.",
  "Твоя зона комфорта и твой рост никогда не пересекаются.",
  "Отказ — это не конец разговора. Это его начало.",
  "Люди следуют за уверенностью. Уверенность приходит от действий.",
  "Не жди идеального момента. Создай его.",
  "Каждый кто сейчас впереди тебя однажды стоял там где ты.",
  "Твой доход растёт ровно настолько насколько растёшь ты.",
  "Система без действия — это просто теория.",
  "Хочешь другие результаты — начни делать то что ты ещё не делал.",
  "Репутация строится годами. Но начинается с одного разговора.",
  "Чем больше людей ты помогаешь достичь цели — тем ближе твоя.",
  "Нет плохих дней. Есть дни когда ты сделал меньше чем мог.",
  "Потенциал без действия — это просто потенциал.",
  "Ты не проигрываешь. Ты учишься или побеждаешь.",
  "Настоящая уверенность — это знание что ты сделаешь следующий шаг независимо от результата.",
  "Большинство людей переоценивают что можно сделать за год и недооценивают за пять лет.",
  "Твоя история ещё не написана. Но каждый день ты делаешь выбор — какой она будет.",
  "Говорить о целях легко. Делать что-то для них — вот это настоящая работа.",
  "Один активный человек в команде стоит десяти пассивных.",
  "Не ищи мотивацию. Создавай результаты — мотивация придёт сама.",
  "Ты можешь выбрать боль дисциплины или боль сожаления. Первая легче.",
  "Успех — это не пункт назначения. Это то как ты движешься каждый день.",
  "Люди не покупают продукт. Они покупают трансформацию.",
  "Если ты не строишь свою мечту — кто-то наймёт тебя строить свою.",
  "Каждое утро у тебя есть выбор — действовать или ждать. Система выбирает действие.",
  "Настоящее партнёрство начинается когда ты веришь в человека больше чем он сам.",
  "Не жалуйся на результаты которые ты не получил от действий которые ты не совершил.",
  "Твоя сеть — это твой капитал. Строй её каждый день.",
  "Последовательность важнее интенсивности. Всегда.",
  "Маленькие победы каждый день создают большие изменения за год.",
  "Самое ценное что ты можешь дать другому человеку — это вера в его возможности.",
  "Делай сегодня то что другие не хотят — и живи завтра так как другие не могут.",
  "Твоя команда видит не то что ты говоришь. Она видит то что ты делаешь.",
  "Скорость роста твоей структуры равна скорости твоего личного роста.",
  "Нет лучшего времени чтобы начать. Есть только сейчас.",
  "Устал — отдохни. Сдался — это другая история.",
  "Каждый человек которому ты помог изменить жизнь — это часть твоего наследия.",
  "Ты не можешь контролировать результат. Но ты можешь контролировать действие.",
  "Разница между теми кто добился и теми кто нет — в том что первые продолжали.",
  "Твоё окружение либо поднимает тебя либо тянет вниз. Середины нет.",
  "Информация без применения — это просто развлечение.",
  "Не объясняй. Делай. Результаты объяснят всё сами.",
  "Каждое приглашение — это подарок. Не все его примут сразу. Но каждое имеет значение.",
  "Рост неудобен. Стагнация тоже. Выбирай дискомфорт который ведёт вперёд.",
  "Ты строишь не структуру в таблице. Ты строишь жизни людей.",
  "Один звонок который ты откладываешь — это возможность которую ты упускаешь.",
  "Лидеры не рождаются. Они формируются через ежедневный выбор действовать.",
  "Твоя история вдохновляет людей больше чем любая презентация.",
  "Когда тебе трудно — вспомни зачем ты начал.",
  "Самые большие изменения происходят когда ты продолжаешь после того как хочется остановиться.",
  "Не жди когда будет легче. Становись сильнее.",
  "Твоё присутствие в системе каждый день важнее одного идеального дня раз в неделю.",
  "Люди чувствуют твою энергию до того как слышат твои слова.",
  "Честность с собой — это начало любого настоящего роста.",
  "Самый важный разговор сегодня — тот который ты ещё не провёл.",
  "Дай людям больше чем они ожидают — и они вернутся.",
  "Путь к свободе не прямой. Но каждый шаг в правильном направлении считается.",
  "Твоя вера в систему передаётся команде. Убедись что она настоящая."
];

const QUESTIONS = [
  "Что ты сделаешь сегодня для своей цели?",
  "Что тебя сейчас тормозит — и что ты можешь с этим сделать?",
  "Если бы успех был гарантирован — сколько людей ты бы пригласил сегодня?",
  "Кто из твоих знакомых сейчас ищет перемен?",
  "Какое одно действие сегодня даст максимальный результат?",
  "Ты строишь команду или ждёшь когда появятся нужные люди?",
  "Что ты сделал вчера что приблизило тебя к цели?",
  "Как ты можешь помочь кому-то в своей команде сегодня?",
  "Что изменится когда ты закроешь следующий ранг?",
  "Какой навык ты развиваешь прямо сейчас?",
  "Если бы ты мог сделать только одно действие сегодня — что бы это было?",
  "Кому ты ещё не написал из своего списка контактов?",
  "Что мешает тебе быть на 10% активнее сегодня?",
  "Как выглядит твой идеальный рабочий день в этом бизнесе?",
  "Что ты узнал на этой неделе что изменило твой подход?",
  "Кто в твоей команде заслуживает твоего внимания прямо сейчас?",
  "Если твоя команда повторяет твои действия — что они делают сегодня?",
  "Что ты откладываешь которое при этом точно знаешь что нужно сделать?",
  "Какой результат ты хочешь видеть через 30 дней?",
  "Ты инвестируешь время в рост или тратишь его на занятость?",
  "С кем из успешных людей тебе стоит провести больше времени?",
  "Что ты можешь делегировать чтобы сфокусироваться на главном?",
  "Как ты оцениваешь свою энергию сегодня по шкале от 1 до 10?",
  "Что тебя вдохновляет в этом бизнесе прямо сейчас?",
  "Кто ждёт от тебя звонка или сообщения сегодня?",
  "Что бы ты посоветовал себе год назад?",
  "Насколько ты сегодня близок к своей цели по сравнению с прошлой неделей?",
  "Есть ли в твоей команде кто-то кто готов сделать следующий шаг но не знает об этом?",
  "Что ты делаешь чтобы твоя команда доверяла тебе?",
  "Как бы выглядела твоя жизнь если бы ты делал это каждый день в течение года?",
  "Что ты знаешь сейчас чего не знал месяц назад?",
  "Ты работаешь над бизнесом или в бизнесе?",
  "Что для тебя значит свобода — и насколько ты к ней близок?",
  "Какой один разговор может изменить ситуацию прямо сейчас?",
  "Ты сегодня сделал что-то чего боялся?",
  "Что останавливает тебя от удвоения текущих действий?",
  "Как ты обычно реагируешь на отказ — и как мог бы реагировать лучше?",
  "Есть ли человек которому ты можешь помочь сегодня независимо от результата для тебя?",
  "Что ты делаешь для своего личного роста помимо бизнеса?",
  "Какие три действия принесли тебе больше всего результатов в этом бизнесе?",
  "Что изменилось в тебе с тех пор как ты начал этот путь?",
  "Если бы страха не было — что бы ты сделал первым?",
  "Ты работаешь из-за цели или из-за давления?",
  "Кого из своей команды ты поддержал на этой неделе?",
  "Что ты можешь улучшить в своём подходе к приглашениям?",
  "Как ты отдыхаешь и восстанавливаешься между активными периодами?",
  "Что для тебя важнее — скорость роста или его качество?",
  "Есть ли у тебя наставник или человек которому ты доверяешь в этом бизнесе?",
  "Что ты делаешь когда не видишь результатов — продолжаешь или ищешь причины?",
  "Какая твоя самая большая победа на этой неделе пусть даже маленькая?",
  "Что ты хочешь чтобы твои партнёры говорили о тебе через год?"
];

function getDayIndex() {
  return Math.floor(Date.now() / 86400000) % THOUGHTS.length;
}

function getDaysAbsent(lastDate) {
  if (!lastDate) return 999;
  return Math.floor((Date.now() - new Date(lastDate).getTime()) / 86400000);
}

// ── BOT ─────────────────────────────────────────
const bot = new TelegramBot(BOT_TOKEN, { polling: true });

bot.onText(/\/start(.*)/, async (msg, match) => {
  const chatId    = String(msg.chat.id);
  const uid       = String(msg.from.id);
  const firstName = msg.from.first_name || 'Партнёр';
  const lastName  = msg.from.last_name  || '';
  const username  = msg.from.username   || '';
  const refCode   = (match[1] || '').trim();

  const token = crypto.randomBytes(20).toString('hex');
  const expires = Date.now() + 10 * 60 * 1000;

  await fsSet(`loginTokens/${token}`, {
    uid, chatId,
    name: (firstName + (lastName ? ' ' + lastName : '')).trim(),
    username,
    refCode,
    expires: String(expires)
  });

  const loginUrl = `${DOJO_URL}?token=${token}${refCode ? '&ref=' + refCode : ''}`;

  await bot.sendMessage(chatId,
    `👋 *Привет, ${firstName}!*\n\n` +
    `Нажми кнопку ниже чтобы войти в DOJO.\n` +
    `Это займёт 3 минуты — и система начнёт работать на тебя.`,
    {
      parse_mode: 'Markdown',
      reply_markup: {
        inline_keyboard: [[{ text: '🚀 Войти в DOJO', url: loginUrl }]]
      }
    }
  );
});

// ── ОТПРАВИТЬ СООБЩЕНИЕ ──────────────────────────
async function sendMsg(chatId, text, btnText = '✓ Открыть DOJO') {
  try {
    await bot.sendMessage(chatId, text, {
      parse_mode: 'Markdown',
      reply_markup: { inline_keyboard: [[{ text: btnText, url: DOJO_URL }]] }
    });
    await new Promise(r => setTimeout(r, 150));
    return true;
  } catch(e) {
    console.error(`Send error ${chatId}:`, e.message);
    return false;
  }
}

// ── CRON: 8:00 МСК (кроме среды) ────────────────
cron.schedule('0 5 * * 0,1,2,4,5,6', async () => {
  console.log('[CRON] Morning...');
  const users  = await getAllUsers();
  const idx    = getDayIndex();
  let sent = 0;
  for (const u of users) {
    const name = u.name.split(' ')[0];
    const streak = u.streak > 1 ? `\n🔥 Серия: *${u.streak} дней* — держи ритм!\n` : '';
    const text =
      `☀️ *Доброе утро, ${name}!*\n\n` +
      `💡 *Мысль дня:*\n_${THOUGHTS[idx]}_\n\n` +
      `❓ *Вопрос на сегодня:*\n${QUESTIONS[idx % QUESTIONS.length]}${streak}`;
    try {
      await bot.sendMessage(u.chatId, text, { parse_mode: 'Markdown' });
      await new Promise(r => setTimeout(r, 150));
      sent++;
    } catch(e) { console.error(`Morning error ${u.chatId}:`, e.message); }
  }
  console.log(`[CRON] Morning: ${sent}/${users.length}`);
}, { timezone: 'UTC' });

// ── CRON: 8:00 МСК среда — цели ─────────────────
cron.schedule('0 5 * * 3', async () => {
  console.log('[CRON] Wednesday goals...');
  const users = await getAllUsers();
  let sent = 0;
  for (const u of users) {
    const g = u.goal || {};
    if (!g.maingoal && !g.dream) {
  const name = u.name.split(' ')[0];
  await sendMsg(u.chatId,
    `📝 *${name}, ты ещё не записал свои цели*\n\n` +
    `Каждую среду партнёры получают напоминание о своих целях и мечтах.\n\n` +
    `Ты пропускаешь это — потому что цели ещё не заполнены.\n\n` +
    `Зайди в DOJO и запиши — займёт 5 минут.\n` +
    `Это то что будет держать тебя в движении когда захочется остановиться 👇`,
    '✓ Заполнить цели'
  );
  sent++;
  continue;
}
    const name = u.name.split(' ')[0];
    let goalLines = '';
    if (g.income)   goalLines += `💰 *Цель по доходу:* ${g.income}\n`;
    if (g.maingoal) goalLines += `🎯 *Главная цель:* ${g.maingoal}\n`;
    if (g.dream)    goalLines += `✨ *Мечта:* ${g.dream}\n`;
    if (g.forwhom)  goalLines += `❤️ *Для кого:* ${g.forwhom}\n`;
    const text =
      `🔄 *${name}, помни зачем ты здесь*\n\n` +
      `Ты написал это сам — в первый день:\n\n` +
      goalLines + `\nКаждое действие сегодня приближает тебя к этому 👇`;
    if (await sendMsg(u.chatId, text, '✓ Открыть DOJO')) sent++;
  }
  console.log(`[CRON] Goals: ${sent}/${users.length}`);
}, { timezone: 'UTC' });

// ── CRON: 19:00 МСК — умные уведомления ─────────
cron.schedule('0 16 * * *', async () => {
  console.log('[CRON] Evening...');
  const users = await getAllUsers();
  let sent = 0;

  for (const u of users) {
    const name = u.name.split(' ')[0];

    if (u.streak === 7) {
      await sendMsg(u.chatId,
        `🔥 *${name}, 7 дней подряд!*\n\nТы прошёл первый барьер.\nБольшинство людей не доходят даже до этого момента.\n\nЭто уже не случайность — это характер.`
      );
      sent++; continue;
    }
    if (u.streak === 14) {
      await sendMsg(u.chatId,
        `⚡ *${name}, 14 дней без пропусков!*\n\nДве недели последовательных действий.\nНейронная связь уже формируется — твой мозг начинает воспринимать это как норму.\n\nПоловина пути к настоящей привычке пройдена.`
      );
      sent++; continue;
    }
    if (u.streak === 21) {
      await sendMsg(u.chatId,
        `🏆 *${name}, 21 день — точка невозврата!*\n\nУчёные называют 21 день минимальным порогом формирования привычки.\nТы его прошёл.\n\nТеперь это часть тебя. Продолжай — дальше становится только легче.`
      );
      sent++; continue;
    }
    if (u.streak === 30) {
      await sendMsg(u.chatId,
        `🥋 *${name}, 30 дней. Это уже образ жизни.*\n\nМесяц ежедневной работы над собой.\nТы входишь в 1% людей которые не просто ставят цели — а следуют системе каждый день.\n\nТвоя команда видит это. Продолжай.`
      );
      if (u.invitedBy) {
        const allUsers = await getAllUsers();
        const leader = allUsers.find(l => l.uid === u.invitedBy);
        if (leader?.chatId) {
          await sendMsg(leader.chatId,
            `🏆 *Твой партнёр достиг 30 дней!*\n\n👤 *${u.name}* только что закрыл серию в 30 дней в DOJO.\nНапиши ему — такие моменты важно отмечать лично.`,
            '📊 Кабинет лидера'
          );
        }
      }
      sent++; continue;
    }

    const absent = getDaysAbsent(u.lastDate);
    let text = null;

    if (absent === 0) {
      text = `📋 *${name}, как прошёл день?*\n\nЗайди и отметь что сделал сегодня — займёт 2 минуты.\nСерия продолжается 👇`;
    } else if (absent === 1) {
      text = `🎯 *${name}, ещё не поздно*\n\nОтметь хотя бы одно действие — и день засчитан.\n2 минуты. Серия продолжается 👇`;
    } else if (absent <= 3) {
      text = `⚡ *${name}, ты пропал на ${absent} дня*\n\nЧто произошло? Серия прервалась, но Momentum ещё можно восстановить 👇`;
    } else if (absent <= 6) {
      text = `🔴 *${name}, уже ${absent} дней без DOJO*\n\nПотерял фокус? Это бывает.\nОдин шаг — и ты снова в системе 👇`;
    } else if (absent === 7) {
      text = `❗ *${name}, прошла целая неделя*\n\n7 дней — это уже не пауза. Это выбор.\nВернись прямо сейчас 👇`;
      if (u.invitedBy) {
        const allUsers = await getAllUsers();
        const leader = allUsers.find(l => l.uid === u.invitedBy);
        if (leader?.chatId) {
          await sendMsg(leader.chatId,
            `⚠️ *Партнёр пропал*\n\n👤 *${u.name}* не заходил в DOJO уже *7 дней*.\nВозможно стоит написать лично.`,
            '📊 Кабинет лидера'
          );
        }
      }
    } else if (absent <= 13) {
      text = `😶 *${name}, тебя нет уже ${absent} дней*\n\nВсё в порядке? Мы здесь 👇`;
    } else if (absent === 14) {
      text = `🤝 *${name}, 2 недели*\n\nНужна помощь? Напиши своему лидеру.\nИли просто открой DOJO — иногда достаточно одного шага 👇`;
      if (u.invitedBy) {
        const allUsers = await getAllUsers();
        const leader = allUsers.find(l => l.uid === u.invitedBy);
        if (leader?.chatId) {
          await sendMsg(leader.chatId,
            `⚠️ *${u.name}* не заходил уже *14 дней*.\nНужна твоя помощь.`,
            '📊 Кабинет лидера'
          );
        }
      }
    } else {
      if (new Date().getDay() !== 1) continue;
      text = `👋 *${name}*\n\nDOJO всё ещё здесь. Возвращайся 👇`;
    }

    if (text && await sendMsg(u.chatId, text, '✓ Заполнить чеклист')) sent++;
  }

  console.log(`[CRON] Evening: ${sent}/${users.length}`);
}, { timezone: 'UTC' });

// ── CRON: 17:00 МСК воскресенье — аудит ─────────
cron.schedule('0 14 * * 0', async () => {
  console.log('[CRON] Sunday audit...');
  const users = await getAllUsers();
  let sent = 0;
  for (const u of users) {
    const name = u.name.split(' ')[0];
    const text =
      `📋 *${name}, время подвести итоги недели!*\n\n` +
      `6 вопросов · 3 минуты · раз в неделю.\n\n` +
      `Аудит помогает видеть рост и не повторять ошибки 👇`;
    if (await sendMsg(u.chatId, text, '📋 Пройти аудит')) sent++;
  }
  console.log(`[CRON] Audit: ${sent}/${users.length}`);
}, { timezone: 'UTC' });

// ── EXPRESS ──────────────────────────────────────
const app = express();
app.use(express.json());

// Keep-alive
app.get('/', (req, res) => res.send('🥋 DOJO Bot · ' + new Date().toISOString()));

// ══════════════════════════════════════════════════
// /notify — принимает запросы от браузера
// Браузер больше не знает BOT_TOKEN
// ══════════════════════════════════════════════════
app.post('/notify', async (req, res) => {
  const { secret, chatId, text, buttonText, buttonUrl } = req.body;

  // Проверяем секрет
  if (!secret || secret !== NOTIFY_SECRET) {
    return res.status(403).json({ ok: false, error: 'Forbidden' });
  }
  if (!chatId || !text) {
    return res.status(400).json({ ok: false, error: 'Missing chatId or text' });
  }

  try {
    await bot.sendMessage(chatId, text, {
      parse_mode: 'Markdown',
      ...(buttonText && buttonUrl ? {
        reply_markup: {
          inline_keyboard: [[{ text: buttonText, url: buttonUrl }]]
        }
      } : {})
    });
    res.json({ ok: true });
  } catch(e) {
    console.error('/notify error:', e.message);
    res.status(500).json({ ok: false, error: e.message });
  }
});

app.listen(process.env.PORT || 3000, () => console.log('🥋 DOJO Bot started'));
